import pygame
#from configs import WINDOW_WIDTH, WINDOW_HEIGHT, WINDOW_DIMENSIONS

"""
SCREEN = pygame.display.set_mode((WINDOW_WIDTH, WINDOW_HEIGHT))
"""
class SQGame:
    def __init__(self, GRID_WIDTH, GRID_HEIGHT, CELL_DIMS): #initial variables specifically for the creation of the grid (add more later)
        pass 

def DrawBoard():
    CELL_DIMS = 32

    for x in range(0, configs.WINDOW_WIDTH, GRIDSIZE):
        for y in range(0, configs.WINDOW_HEIGHT, GRIDSIZE):
            continue 